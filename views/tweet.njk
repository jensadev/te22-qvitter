{% extends "layout.njk" %}

{% block content %}
<main class="container region flow">
    <article>
        <header>
            <h1>{{ tweet.name }}</h1>
            <time datetime="{{ tweet.date }}">{{ tweet.date }}</time>
            <p>{{ tweet.message }}</p>
        </header>
    </article>
</main>
{% endblock %}